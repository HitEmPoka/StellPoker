[package]
name = "split_pot_valid"
type = "bin"
authors = ["Stellar Poker"]
compiler_version = ">=0.36.0"

[dependencies]
stellar_poker_lib = { path = "../lib" }

[package.metadata.circuit]
description = "Proves correct multi-way all-in side-pot split ratios and eligibility in Texas Hold'em (Issue #532)."
max_players = 6
