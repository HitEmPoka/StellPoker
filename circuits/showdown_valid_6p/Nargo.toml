[package]
name = "showdown_valid_6p"
type = "bin"
authors = ["Stellar Poker"]
compiler_version = ">=0.36.0"

[dependencies]
stellar_poker_lib = { path = "../lib" }

[package.metadata.circuit]
description = "Parameterised showdown_valid for 6 players (Issue #7)."
max_players = 6
